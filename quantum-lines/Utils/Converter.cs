using System;
using System.Collections.Generic;
using MatrixDotNet;
using quantum_lines.Operators;

namespace quantum_lines.Utils
{
    public static class Converter
    {
        public static Matrix<float> LinesToMatrix(List<OperatorsLine> lines)
        {
            // надо еще прочекать надо ли это и сделать если надо, а если не надо, то поменять
            throw new NotImplementedException();
        }
        
        public static List<OperatorsLine> MatrixToLines(Matrix<float> matrix)
        {
            // надо еще прочекать надо ли это и сделать если надо, а если не надо, то поменять
            throw new NotImplementedException();
        }
    }
}